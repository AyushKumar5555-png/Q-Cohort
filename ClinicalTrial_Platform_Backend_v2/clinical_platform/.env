# Backend env — copy this to .env and fill in values
# SQLite (zero setup, works immediately)
DATABASE_URL=sqlite:///./clinical_trial_platform.db

# PostgreSQL (production — uncomment and fill)
# DATABASE_URL=postgresql://username:password@localhost:5432/clinical_trial_db

HOST=0.0.0.0
PORT=8000
